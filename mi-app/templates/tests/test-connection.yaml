apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mi-app-danae.fullname" . }}-test-connection"
  labels:
    {{- include "mi-app-danae.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mi-app-danae.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
